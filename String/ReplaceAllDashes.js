// 5. Replace All Dashes – Clean Up Input

let urlTitle = "learn-javascript-by-building-projects";

// using replaceAll(what,with)
let cleanInput = urlTitle.replaceAll("-", " ");
console.log("Clean Input :", cleanInput);
